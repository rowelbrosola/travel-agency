/*
 * Adventures Messages
 *
 * This contains all the text for the Adventures container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Adventures';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Adventures container!',
  },
});
