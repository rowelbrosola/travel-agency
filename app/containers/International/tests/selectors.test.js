// import { selectInternationalDomain } from '../selectors';

describe('selectInternationalDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
